# ENCOGAS - Backend

## Requisitos
- Java 21
- MySQL (local)
- Maven

## Config local
Crear DB `encogas` (o dejar createDatabaseIfNotExist=true).
Editar `application-local.yml` si tu usuario/pass no es root/root.

## Run
```bash
mvn spring-boot:run